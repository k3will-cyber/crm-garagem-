{
  "name": "crm-garagem",
  "version": "1.0.0",
  "description": "CRM Garagem - Painel Administrativo",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  }
}
